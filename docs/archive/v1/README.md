# AgentMentor v1 历史归档

此目录保存 `v1.0.0` 稳定基线的 Feature、ADR、Lesson 与 Evidence。它们用于人工追溯历史设计，不属于 AgentMentor vNext 的运行期 Schema，也不会被 vNext 的 `agentmentor context` 或严格校验器扫描。

对应发布标签：`v1.0.0`（commit `b7b2bec75bf4c91d644394b16b16e0a2fed91247`）。
