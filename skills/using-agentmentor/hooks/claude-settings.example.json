{
  "hooks": {
    "Stop": [
      {
        "matcher": "",
        "hooks": [
          {
            "type": "command",
            "command": "python -c \"import os, subprocess, sys; runner = os.path.join(os.environ['AGENTMENTOR_SKILL_ROOT'], 'hooks', 'agentmentor_hook.py'); sys.exit(subprocess.call([sys.executable, runner, '--event', 'stop', '--platform', 'claude']))\""
          }
        ]
      }
    ]
  }
}
